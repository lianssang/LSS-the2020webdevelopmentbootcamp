<%- include('partials/header') %>

<h1>Home</h1>
<p><%= contentHome %></p>

<% contentPosts.forEach(function(content) { %>
<h1><%= content.title %></h1>
<p><%= content.body.substring(0, 100) + '...' %>
    <a href="/posts/<%= content.title %>">Read More</a>
</p>
<% }); %>

<%- include('partials/footer') %>